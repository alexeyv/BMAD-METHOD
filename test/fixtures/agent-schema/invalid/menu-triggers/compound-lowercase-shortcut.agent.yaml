# Test: Compound trigger with lowercase shortcut
# Expected: FAIL
# Error code: custom
# Error path: agent.menu[0].trigger
# Error message: agent.menu[].trigger compound format error: invalid compound trigger format

agent:
  metadata:
    id: compound-lowercase-shortcut
    name: Lowercase Shortcut
    title: Lowercase Shortcut Test
    icon: 🧪

  persona:
    role: Test agent
    identity: Test identity
    communication_style: Test style
    principles:
      - Test principle

  menu:
    - trigger: ts or fuzzy match on tech-spec
      description: Shortcut must be uppercase (1-3 capital letters)
      action: test
