# Test: Compound trigger with non-kebab-case in fuzzy match
# Expected: FAIL
# Error code: custom
# Error path: agent.menu[0].trigger
# Error message: agent.menu[].trigger compound format error: invalid compound trigger format

agent:
  metadata:
    id: compound-invalid-fuzzy-kebab
    name: Invalid Fuzzy Kebab
    title: Invalid Fuzzy Kebab Test
    icon: 🧪

  persona:
    role: Test agent
    identity: Test identity
    communication_style: Test style
    principles:
      - Test principle

  menu:
    - trigger: TS or fuzzy match on TechSpec
      description: Fuzzy match target must be kebab-case, not camelCase
      action: test
